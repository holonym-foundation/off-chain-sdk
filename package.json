{
  "main": "bulid/index.js",
  "scripts": {
    "build": "npx tsc && cp -r src/types/ build/types/",
    "watch": "npx tsc -w && cp -r src/types/ build/types/"
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "axios": "^1.3.4",
    "zokrates-js": "1.1.4"
  }
}
